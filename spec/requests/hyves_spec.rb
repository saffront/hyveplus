require 'rails_helper'

RSpec.describe "Hyves", :type => :request do
  describe "GET /hyves" do
    it "works! (now write some real specs)" do
      #get hyves_path
      expect(response.status).to be(200)
    end
  end
end
